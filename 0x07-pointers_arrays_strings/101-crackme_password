#include "main.h"
#include <stdio.h>

/**
* main - check the code

* Return : Always 0.
*/
int main (void)
{
	char *s0 = "Sam Atsen"
	char *s1 = "Emma Itse"

	prinf("%s , %s\n", s0, s1);
	set_string(&s1 , s0);
	printf("%s , %s\n" , s1);
	return (0);
}
